version: "3"
sevices:
  jupyter:
    build: .
    image: my-pytorch-img
    container_name: "my-pytorch"
    command:
      jupyter-lab --ip 0.0.0.0 --allow-root --LabApp.token=''

    ports:
      - "8888:8888"
    volumes:
      - ..:/work
